Renderer has 2 ways of communicating with the engine. A draw list (description of the scene and what it needs to be rendered) and the renderer backend.

Render backend is like BackendMesh so like. you say drawlist->add(backendmesh).

# Platform shader strategy

still not sure i have to research how unreal makes the rendering so high level. like shaders should be declared how i did the shaders for the block a y3

Would also be awesome to have the ability to chose 